<!DOCTYPE html>
<html>
  <head>
    <title>Traveller's Paradise</title>
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="/stylesheets/landing.css" />
    <script
      src="https://cdnjs.cloudflare.com/ajax/libs/modernizr/2.8.3/modernizr.min.js"
      type="text/javascript"
      async
    ></script>
  </head>
  <body>
    <div class="container">
      <% if(errorFlash && errorFlash.length > 0){ %>
      <div class="alert alert-danger" role="alert">
        <%= errorFlash %>
      </div>
      <% } %> <% if(successFlash && successFlash.length > 0){ %>
      <div class="alert alert-success" role="alert">
        <%= successFlash %>
      </div>
      <% } %>
    </div>

    <div id="landing-header">
      <h1>Welcome to Traveller's Paradise!</h1>
      <a href="/touristspots" class="btn btn-lg btn-info"
        >View All Exploration Points</a
      >
    </div>

    <ul class="slideshow">
      <li></li>
      <li></li>
      <li></li>
      <li></li>
      <li></li>
    </ul>
  </body>
</html>
